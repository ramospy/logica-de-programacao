algoritmo "soma_grande2"
var

  s,k: inteiro

inicio

 k <- 50
 s <- 0

  Enquanto (k <= 500) faca
    s <- s + k
    k <- k + 50
  FimEnquanto

 Escreval ("O resultado desta soma é igual a: ",s)

fimalgoritmo