algoritmo "calculadora_taxa"
var

   vp,tx: real

inicio

      Escreva ("Qual o valor total dos produto(s) ? R$")
      Leia (vp)

      tx <- (60/100) * vp
      
      Escreva ("O valor da taxa será de:R$",tx)

fimalgoritmo